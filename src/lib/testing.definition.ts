/**
 * this is how to test any components using shadcn component
 * since the component is to provide with dynamic types
 * So we mock any import using vi.mock
 */

// beforeEach(() => {
// 	vi.mock("../ui/alert", () => ({
// 		Alert: ({ children }: any) => <div aria-label="alert">{children}</div>,
// 		AlertTitle: ({ children }: any) => (
// 			<div aria-label="alert-title">{children}</div>
// 		),
// 		AlertDescription: ({ children }: any) => (
// 			<div aria-label="alert-description">{children}</div>
// 		),
// 	}));
// });
